name: Canonical Checks

on:
  push:
    branches:
      - main
      - master
      - work
  pull_request:

jobs:
  checksum:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Verify checksum manifest is in sync
